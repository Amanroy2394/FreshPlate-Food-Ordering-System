# Default MySQL settings (override with env EHPT_* or a file db.local.properties next to pom.xml).
# See db.local.properties.example — copy to db.local.properties and set jdbc.password (restart server).
jdbc.url=jdbc:mysql://localhost:3306/ehpt_food?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
jdbc.username=root
jdbc.password=@aman1234
